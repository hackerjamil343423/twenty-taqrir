diff --git a/node_modules/@react-pdf/textkit/lib/textkit.js b/node_modules/@react-pdf/textkit/lib/textkit.js
index 0b8b6fa..b4d31f9 100644
--- a/node_modules/@react-pdf/textkit/lib/textkit.js
+++ b/node_modules/@react-pdf/textkit/lib/textkit.js
@@ -1030,7 +1030,7 @@ const reorderLine = (line) => {
         for (let i = 0; i < selectedIndices.length; i += 1) {
             const index = selectedIndices[i];
             const glyph = getItemAtIndex(line.runs, 'glyphs', index);
-            if (addedGlyphs.has(glyph.id))
+            if (!glyph || addedGlyphs.has(glyph.id))
                 continue;
             updatedGlyphs.push(glyph);
             updatedPositions.push(getItemAtIndex(line.runs, 'positions', index));
